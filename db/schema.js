// Версия схемы базы. Увеличивается, когда меняется структура таблиц в init.js
// так, что старая база с ней несовместима. db/index.js сравнивает её с
// PRAGMA user_version в файле базы и при расхождении пересоздаёт базу
// (старый файл сохраняется рядом как резервная копия).
//   1 — вход по логину
//   2 — вход по почте, бан по почте, до 10 фото, фото у демо-товаров
//   3 — характеристики объявлений (products.attributes), журнал модерации
module.exports = { SCHEMA_VERSION: 3 };
